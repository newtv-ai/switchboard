# Switchboard 多 AI 协作 —— 可落地执行方案 (v2)

> 来源：《下一步更新计划.txt》10 条想法 · 编制 2026-06-24 · 修订 2026-06-25
> 本版**已逐条核对真实代码 + 核对竞品**，纠正了 v1 草案里若干站不住脚的前提。
> 落地前每项功能须按 SPEC §11 纪律先在 SPEC.md 落一节。

---

## 0. 两项核对结论（决定方向，先看这里）

### 0.1 代码现状核对（纠正 v1 草案的错误前提）

v1 草案有几处假设与真实代码不符，会让我们走弯路。已逐一核对：

| v1 草案的假设 | 真实代码 | 对方案的影响 |
|---|---|---|
| "adapter 的 parser 已能发 `usage` 事件" | **没有任何适配器实现 `createParser`**。`usage` 只是 `packages/sdk/src/index.ts:81` 里的一个类型，无人发射。 | **想法#9 token 自动切换在当前代码里没有任何数据源** → 本版已降级为「手动交接按钮」。 |
| "`install.detect()` 已定义" | 类型 `InstallInfo` 定义了（sdk index.ts:38-48），但 **codex/antigravity/passthrough 三个适配器都没写 `install` 字段** | 想法#2 的 CLI 扫描要**从零实现**（PATH 探测 + 给每个适配器补 detect），不是"接上已有的"。 |
| Claude 是一等适配器 | **没有 claude 适配器**。`server.ts:38-40` 只注册了 `passthrough`/`codex`/`antigravity`，claude 走 `passthrough`。 | 共享上下文注入不能假设每个 AI 都有专属 adapter 钩子；P1 需要新增 claude 适配器。 |
| 会话里有结构化文本 | 主用模式是 wrapped（`switchboard run`），`session-manager.ts:89` 写死 `enableParser: false`，是裸 ANSI TUI 字节流 | "读另一个 AI 的输出" = 从 ring buffer 取**裸终端字节**再 strip-ansi，不是干净文本。 |

**一句话**：结构化（L2）层目前是"只有类型、没有实现"。所有依赖结构化事件的设计（token 监测、干净文本互读）都要改成**文件协议 + ring buffer 裸字节**的务实路线。

### 0.2 竞品核对（想法#1 明确要求"一定要搜索"）

2026 年这个赛道已经成型，社区称为 **"agentmaxxing"**（多厂 AI CLI 并行）。已实现你想法#1–#3 大部分的现成项目：

| 项目 | 做法 | 和我们的重合点 | 它缺什么（=我们的机会） |
|---|---|---|---|
| **CCB / claude_code_bridge** | tmux 多 pane 跑 Codex/Claude/Gemini/Kimi/Qwen；`.ccb/ccb_memory.md` 共享记忆；`/ask reviewer` 任务委派 + 回调依赖；git worktree 隔离 | 几乎等于想法#1+#3 | 手机端只有 "Mobile Gateway **Alpha**"（websocket 终端帧），不成熟 |
| **amux** | Web dashboard + REST + 移动 PWA + kanban 任务板 + SSE 实时 | 任务板 + PWA | 偏后端编排，非"手机优先 PTY 中继" |
| **codeagentswarm / kodo / vibe-kanban / gnap** | 并行 CLI、kanban、git 仓库当 todo/doing/done 看板 | 任务看板、共享状态 | 都不是手机遥控形态 |

**关键结论（务必记住，避免重复造轮子）**：
1. 大家收敛出的共识原语是 **PTY/tmux pane + git worktree 隔离 + Markdown 文件当共享上下文协议 + kanban 任务板**。你"共享文档/记忆"的直觉是对的，业界都这么做——所以**别自创协议，照抄 Markdown 文件协议**。
2. Switchboard 的**真正差异化**是别人只有 alpha 的能力：**手机优先的成熟 PTY 中继 + PWA + Tailscale 远程**。方案应把"手机遥控多 AI"做厚，而不是在编排算法上和 CCB/amux 硬拼。

来源见文末 §8。

---

## 1. 战略定位

> 你已选定：**全量分阶段 P0–P5**。本方案据此展开。

Switchboard 现在是"**一部手机管所有 AI CLI**"（PTY 中继 + 会话管理 + adapter 插件）。本方案把它从"**多会话并列、手动切换**"升级为"**多 AI 协同、手机可遥控的工作群**"。差异化锚点始终是**手机端**——每个阶段的验收都要求"在手机上能用"。

不做（沿用 SPEC §1.2 非目标，防范围蔓延）：多用户、E2E 加密、云托管、LLM API 网关。多 AI ≠ 多用户，工作群仍是单用户单机。

---

## 2. 架构演进

```
现状:  SessionManager ──┬── Session(wrapped, claude/passthrough)
                        ├── Session(wrapped, codex)
                        └── Session(spawned, ...)

目标:  WorkgroupManager ── Workgroup
          │                  ├── members: AgentMember[]  → 复用现有 Session
          │                  ├── sharedContext (磁盘文件协议)
          │                  ├── tasks: Task[]
          │                  ├── workflow: WorkflowState
          │                  └── timeline: Event[] (JSONL)
          └── 持久化到 ~/.switchboard/workgroups/<id>/
       SessionManager 不动（Session 仍是底层单元）
```

`WorkgroupManager` 是**叠加在 `SessionManager` 之上的薄层**，不重写会话/PTY/中继。每个 AI 成员仍是一个普通 `Session`；工作群只额外维护"谁在群里、共享目录在哪、任务清单、时间线"。

### 2.1 三个贯穿全程的关键设计决策

1. **共享上下文 = 磁盘 Markdown 文件协议**（抄业界共识，不自创）。每个工作群一个目录 `~/.switchboard/workgroups/<id>/`，放 `context.md`/`decisions.md`/`handoff.md`/`artifacts/`/`timeline.jsonl`。AI 通过"读这些文件"共享记忆——**不改 AI CLI 本身**。
2. **跨 AI 读取 = ring buffer peek（只读，不 attach）**。想法#3 的"获取另一个 AI 工作页面的信息"分两层：
   - 被动（v1）：所有产物写进 `artifacts/`，任何成员都能读。
   - 主动（进阶）：新增 `SessionManager.peek(sessionId, lines)` 读目标会话 ring buffer 最近 N 行，**用现成的 `strip-ansi` 去掉控制码**（已是依赖），返回近似纯文本。
3. **写共享文件串行化**。多 AI 同时写同一文件会损坏上下文 → 共享目录所有写操作走服务端单写者队列；或按 agent 分子目录、合并时由调度器/用户裁决。worktree 隔离（每个执行型任务一个 `git worktree`）天然减少冲突。

### 2.2 上下文注入：怎么让 AI"读到"共享记忆

`SpawnOpts.initialPrompt`（sdk index.ts:61）已定义但 `spawn()` 没传。注入分两条腿，按可靠性叠加：

- **腿 A（文件，最稳，跨所有 CLI）**：在工作群的 worktree 里写好 `context.md`，并在该目录的项目指令文件（`CLAUDE.md` / `AGENTS.md` / `GEMINI.md`，各 CLI 约定位置）里写一句"开工前先读 `.switchboard/context.md`"。这是 CCB/codeagentswarm 验证过的"Markdown 当协议"做法。
- **腿 B（initialPrompt，锦上添花）**：把 `spawn()` → `buildCommand()` 的 `initialPrompt` 真正接通，会话 ready 后把"本群目标 + 你的角色 + 共享目录路径"写进 stdin。passthrough 模式退化为"打印一行提示让用户自己贴"。

---

## 3. 分阶段路线图 P0–P5

每阶段给：目标 / 新增·改动文件 / 关键数据结构或协议 / **验收闸门** / 粗估工作量 / 风险。闸门沿用 SPEC 的闸门纪律——过不了不进下一阶段。

### P0 — 文档规范 + 竞品推荐（想法#4、#10）｜~0.5 天

**目标**：先把"规范"立起来，后续每阶段的规划/审计文档有处可放。

> ⚠️ **已核对**：`.gitignore:58` 把**整个 `docs/` 忽略**（"Anything in docs/ stays" 本地）。因此区分**本地工作笔记**与**公众可见文档**两类，避免 README 链到被忽略的文件（GitHub 上 404）。

**动作**：
- `docs/` 作为**本地工作笔记**目录（不进 git，沿用现状），细分：
  ```
  docs/
  ├── planning/        ← 规划讨论、本方案副本、roadmap（本地）
  ├── audit/           ← 审计报告 + audit-report-template.md（本地）
  └── architecture/    ← ADR、安全模型草稿（本地）
  ```
- **公众可见文档（被 git 跟踪）**：
  - 想法#10 的"相关项目推荐" → 直接写进 **`README.md` + `README.zh-CN.md` 的"相关项目 / Related Projects"章节**（不放 `docs/`，否则 GitHub 不可见）；完整竞品表见本方案 §8。
  - 项目级 agent 指令放**根目录**（各 CLI 约定位置，被跟踪）：`CLAUDE.md`（项目级，区别于全局 `~/.claude/CLAUDE.md`）、`AGENTS.md`（Codex/通用约定）、`GEMINI.md`（如用 Gemini CLI）。内容：项目架构概览、代码规范、常用命令、注意事项。
- **规划草案合并进度**：本文件为唯一权威版。`下一步更新计划.txt` **已删除**；`项目改进与优化计划.md` 待删除（被自动模式拦，需你一句确认）。

**闸门**：`docs/{planning,audit,architecture}` 建好（本地）；README（中英）有"相关项目"章节；根目录有项目级 `CLAUDE.md`/`AGENTS.md`。

**风险**：低。注意 SPEC §7 仓库结构要同步更新，并写明 `docs/` 为本地约定。

---

### P1 — CLI 自动扫描（想法#2 的扫描部分）｜~1–1.5 天

> **状态 2026-06-26：✅ 完成并验证。** 实测 `/api/scan` 返回 claude(v2.1.191)/codex(v0.133.0)/antigravity(v1.0.1)，含路径与版本。typecheck + biome + build 全绿。
> **两处与原计划的偏差（有意为之）**：
> 1. 用 **REST `GET /api/scan`** 而非 WS `scan/scan.result` 消息——与既有 `/adapters`、`/sessions` 一致，只读调用更简单。
> 2. **手动触发**（前端"Agents"按钮打开即扫描）而非开机自动扫——契合"默认不自动启动进程"的风险缓解。
> **新增/改动文件**：`detect.ts`、`adapters/claude.ts`(新建缺失适配器)、`cli-scanner.ts`、`adapters/codex.ts`+`antigravity.ts`(补 detect)、`server.ts`(注册 claude + `/api/scan`)、`web/CliScan.tsx`、`web/SessionList.tsx`(Agents 按钮)。

**目标**：启动后/手动触发，扫描本机有哪些 AI CLI，前端展示"可用 AI"列表。这是工作群"成员来源"的前置。

**新增·改动**：
- 给每个适配器补 `manifest.install.detect()`（目前全缺）：
  - `codex.ts`、`antigravity.ts`：实现 detect（`which`/`where` + `--version`）。
  - **新增 `packages/server/src/adapters/claude.ts`**（当前不存在 claude 适配器），实现 buildCommand + detect。注册进 `server.ts`。
- 新增 `packages/server/src/cli-scanner.ts`：并发调用所有已注册 adapter 的 `detect()`；再**额外探测无 adapter 的常见命令**（`gemini`/`qwen`/`opencode`/`aider`/`cursor`），探测到的以 `passthrough` 标记"无专属 adapter"。
- 协议加 `scan` / `scan.result` 消息（`protocol.ts` 两端）。

**数据结构**：
```typescript
interface CliScanResult {
  adapterId: string;        // "claude" | "codex" | "gemini" | ...
  command: string;
  path?: string;
  version?: string;
  hasAdapter: boolean;      // false → 走 passthrough
  status: "available" | "missing" | "error";
}
```

**闸门**：手机首页点"扫描"→ 列出本机所有 available 的 AI CLI（含版本/路径）；探测到无 adapter 的 CLI 也能显示并标注。

**风险**：跨平台 PATH/版本解析差异（Win `where` vs *nix `which`；各 CLI `--version` 格式不一）。对策：detect 失败降级为 `status:"error"` 不阻塞扫描。**默认不自动启动任何进程**（仅扫描+展示），避免静默起一堆进程。

---

### P2 — Workgroup 抽象 + 共享上下文（想法#1 核心）｜~3–4 天

> **状态 2026-06-26：后端 ✅ 完成并验证；前端 + 实时时间线 ⏳ 待做。**
> - 成员模型按你选的 **Option B**：`POST /api/workgroups/:id/members {adapterId}` 直接在工作群的 `cwd` 里 server-spawn 一个 CLI 会话（"用户想用时在该文件夹起 CLI"）。
> - 实测：建群 → 加 passthrough 成员（确认真起了会话, role=active）→ GET memberCount=1 → 共享目录 `artifacts/ context.md decisions.md handoff.md timeline.jsonl workgroup.json` 自动生成 → timeline 记录 `workgroup.created`/`member.joined`。typecheck + biome + core build 全绿。
> - **新增文件**：`core/workgroup.ts`(模型,导出)、`server/shared-context.ts`(文件协议+单写者串行队列)、`server/workgroup-store.ts`(持久化 `~/.switchboard/workgroups/<id>/`)、`server/workgroup-manager.ts`；`server.ts` 加 6 个 `/api/workgroups*` REST 端点。重启 `init()` 按 SPEC §9 剪掉已死会话的成员。
> - **前端 ✅ 完成**：`WorkgroupList.tsx`(建群——填项目文件夹 cwd——+ 群列表)、`WorkgroupView.tsx`(成员 role 一键切 active→observer→idle、用 P1 扫描结果一键加成员、打开成员终端、移除)、`SessionList` 加 "Workgroups" 入口、`App` 加 `workgroups`/`workgroup` 两个持久化视图。typecheck + biome + web build 全绿(浏览器点测留待审计)。
> - **③ 上下文注入 ✅ 完成(2026-06-26)**：共享上下文落到**项目内 `<cwd>/.switchboard/`**(成员能直接读);建群时种入 `context.md`(含群名/路径/目标占位)并往项目 `AGENTS.md`+`CLAUDE.md` 注入**带标记、幂等的管理块**指向 `.switchboard/`——靠各 CLI 自动读指令文件,**不依赖 stdin 时序**(腿 A,采 CCB `.ccb/` 共识)。顺带两个多项目增强:**建群默认名 = 文件夹 basename**、**按 cwd 去重/续用**(同项目记忆持续累积)。实测全过:basename 命名、dedupe=True、`.switchboard/` 文件生成、`AGENTS.md`/`CLAUDE.md` 注入。typecheck+biome 全绿,测试产物已清理。
> - **待做**：② `/ws` 加 `workgroup.*` 实时广播(现靠手动 Refresh);多项目剩余小项(最近文件夹选择器、SessionList 会话归属标注)。

**目标**：能建工作群、把 ≥2 个 AI 加入、共享 `context.md`，手机上有"群聊时间线 + 成员开关"。

**新增·改动**：
- `packages/core/src/workgroup.ts`：`Workgroup` / `AgentMember` 模型。
- `packages/server/src/workgroup-manager.ts`：建群/列群/邀请/设角色，叠加在 SessionManager 上。
- `packages/server/src/workgroup-store.ts`：持久化到 `~/.switchboard/workgroups/<id>/`（JSON + JSONL）。**服务端重启可恢复工作群元数据**（但活的 PTY 会话不持久，对齐 SPEC §9 Q3）。
- `packages/server/src/shared-context.ts`：共享目录文件协议 + **单写者串行化队列**。
- 接通 `initialPrompt` 注入（§2.2 腿 A+B）。
- 前端新增 `packages/web/src/WorkgroupList.tsx` + `WorkgroupView.tsx`；首页导航加"工作群"入口。
- 协议加 `workgroup.create|list|invite|setRole|event`（`protocol.ts` 两端）。

**数据结构**：
```typescript
interface Workgroup {
  id: string; name: string; createdAt: number;
  contextDir: string;                 // ~/.switchboard/workgroups/<id>/
  members: AgentMember[];
}
interface AgentMember {
  sessionId: string; adapterId: string;
  role: "active" | "observer" | "idle";   // 想法#1 的"选择框"= 切这个 role
  joinedAt: number;
}
```
共享目录布局：
```
~/.switchboard/workgroups/<id>/
├── context.md       ← 当前共享上下文摘要（注入各 AI）
├── decisions.md     ← 决策记录
├── handoff.md       ← 交接笔记（P5 用）
├── artifacts/       ← 共享产物（diff/报告/截图）
└── timeline.jsonl   ← 结构化事件流（前端渲染时间线）
```

**前端 WorkgroupView**：顶部成员列表（每个 AI 头像 + 状态点：绿=active/蓝=observer/灰=idle）+ 勾选框切 role；中部把多个 AI 输出按时间合并成"群聊时间线"；底部输入框 + "分派给…"（P3）。

**闸门**：建群→拉 claude+codex 入群→`context.md` 被两个 AI 读到→手机上看到合并时间线→服务重启后工作群还在。

**风险**：多 AI 并发写共享文件冲突（→ 串行化队列）；时间线合并多路 ANSI 流的渲染量大（→ 节流 + 复用现有 ring buffer replay 机制）。

---

### P3 — 任务分派界面 + 跨 AI 读取（想法#3）｜~3 天

> **状态 2026-06-26：✅ 完成(后端实测 + 前端静态验证)。**
> - 后端:`task-store.ts`(Task 模型 + 持久化 `tasks.json` 到注册表)、`task-manager.ts`(建/列/分派/状态)、`session-peek.ts`(读 ring buffer 去 ANSI 近似文本);`server.ts` 加 5 个 REST(tasks 列/建 + assign + status + `GET /api/sessions/:id/peek`)。
> - **分派** = 把任务写进被分派会话的 PTY stdin(成员已在该文件夹运行,等于替用户"打字 + 回车")。**跨 AI 读取** = peek(只读、近似)。
> - 前端 `WorkgroupView` 加任务区:建任务、选成员 **Dispatch**、Done/Fail、每个成员 **Peek** 看近期输出。
> - 实测:task `pending`→assign(`running` + 真写入会话)→peek 返回 611 字符→`done`(completedAt)。typecheck+biome+build 全绿,测试产物已清理。
> - **偏差/限制**:REST 非 WS;peek 是裸 TUI 近似(非渲染屏);任务 `dependencies`/worktree 隔离暂未做。

**目标**：群里发布任务→分派给某 AI→AI 执行（能读共享上下文）→结果写回共享目录→群内广播。

**新增·改动**：
- `packages/server/src/task-store.ts`：Task CRUD + 状态机 + 持久化。
- 分派 = 把"任务描述 + 共享上下文路径"写进被分派 AI 的 PTY stdin（复用 `input`）。
- **跨 AI 读取**：`SessionManager.peek(sessionId, lines)`（只读 ring buffer + strip-ansi）+ 协议 `session.peek`。v1 先被动共享（artifacts/），主动 peek 作为本阶段附加。
- 每个执行型任务可选起一个 `git worktree`（业界共识隔离原语），产物隔离、便于审计。
- 前端 `WorkgroupView` 加"待办/进行中/已完成"三列看板 + "分派给…"下拉。

**数据结构**：
```typescript
interface Task {
  id: string; workgroupId: string;
  title: string; description: string;
  assignee?: string;            // sessionId
  status: "pending"|"assigned"|"running"|"done"|"failed"|"blocked";
  dependencies?: string[];      // A 完成 B 才能开始
  worktree?: string;            // 可选隔离目录
  result?: string;              // 结果摘要 → 写 artifacts/
  createdAt: number; assignedAt?: number; completedAt?: number;
}
```

**分派策略**（先 manual，其余可选）：`manual` / `round-robin` / `least-loaded` / `capability-match`（按 adapter `capabilities`）。

**闸门**：手机上发任务→分派给 codex→codex 读到 context.md 执行→结果写入 artifacts/→时间线广播"codex 完成任务 X"；再让 claude `peek` 到 codex 的近期输出。

**风险**：把任务塞 stdin 对 TUI 型 CLI 时机敏感（要等 ready 且在输入态）→ 复用现有 wrapper "ready 检测"；peek 的裸 ANSI 清洗不完美 → 标注"近似"。

---

### P4 — 四步工作流模板（想法#5/#6/#7/#8）｜~3–4 天

> **状态 2026-06-26：✅ 完成(后端实测 + 前端静态验证)。做的是精简版**——工作流是个"阶段跟踪器",不是独立执行引擎:进入每个阶段就用 P3 的任务系统自动建一条该阶段的模板任务,实际干活仍走任务分派。
> - 后端:`workflow.ts`(阶段顺序 planning→execution→audit→bugfix→done + 四个阶段模板文案 + assignee 建议)、`workflow-manager.ts`(start/advance,持久化 `workflow.json` 到注册表);`server.ts` 加 3 个 REST(get/start/advance)。
> - 前端:`WorkgroupView` 加 "Workflow (SOP)" 区,阶段面包屑高亮当前 + Start/Advance/Restart。
> - 实测:start→planning(建 1 任务)→advance 依次 execution(2)/audit(3)/bugfix(4)/done(4,不再建),四条任务标题正是四阶段模板。typecheck+biome+build 全绿,测试产物已清理。
> - **偏差/精简**:阶段任务**默认不自动分派**(只创建,审计阶段在文案里建议"交叉审计给未执行成员",由用户分派);无依赖图/自动推进/自动回归。

**目标**：把"规划→执行→审计→修 bug"固化为可复用模板，群里一键启动、按步推进、状态可恢复。

**新增·改动**：
- `packages/server/src/workflows/{planning,execution,audit,bugfix}.ts`，各导出 `WorkflowTemplate`。
- 工作流状态持久化 `~/.switchboard/workgroups/<id>/workflow.json`（重启可恢复）。
- 前端加"启动工作流"按钮 + 步骤状态指示。

**四模板要点**：
1. **规划**：用户发需求 → 全体 active AI 并行给方案 → 互相 review → 用户拍板 → 写 `decisions.md` + 自动拆出 `Task[]`。
2. **执行**：按 `dependencies` 调度 → 各 AI 执行 → 产物写 `artifacts/` → 完成触发依赖任务。
3. **审计**：**交叉审计**（让没参与执行的 AI 审，避免自审）→ 汇总 diff → 输出问题清单+严重级 → 写 `docs/audit/<日期>-audit.md`。
4. **修 bug**：建 bug 任务关联审计位置 → 分派修复 → 触发"回归审计"（只审改动文件）→ 通过则关闭。

```typescript
interface WorkflowTemplate { id: string; displayName: string; steps: WorkflowStep[]; }
interface WorkflowStep {
  id: string; name: string;
  assigneeStrategy: "all-active"|"non-executors"|"manual"|"specific";
  inputs: string[]; outputs: string[];
  autoAdvance: boolean;
}
```

**闸门**：在一个工作群里跑通一次完整"规划→执行→审计→修 bug"，每步产物落到对应文件/目录。

**风险**：自动推进易失控 → 默认每步之间要用户确认（`autoAdvance:false`），强成功标准的步骤才允许自动跳。

---

### P5 — 手动交接按钮（想法#9 的落地版）｜~1.5 天

> **状态 2026-06-26：✅ 完成(后端实测 + 前端静态验证)。**
> - 后端:`shared-context.ts` 加 `appendHandoff`;`WorkgroupManager.handoff(group, from, to, note)`——写 `handoff.md`(时间戳+from→to+笔记)、翻转角色(from→idle / to→active)、给 to 会话 stdin 发"接手提示:读 handoff.md + context.md 继续";`server.ts` 加 `POST /api/workgroups/:id/handoff`。
> - 前端:`WorkgroupView` 每个成员加 **Handoff** 按钮 → 弹面板(选目标成员 + 写交接笔记)→ Confirm。
> - 实测:handoff A→B 后 from.role=idle、to.role=active,`handoff.md` 写入带笔记的条目。typecheck+biome+full build 全绿,测试产物已清理。

> 你已决定：**不做 token 自动切换**（当前无 token 数据源）。改为手动交接。

**两种切换场景**（详见 §9 借鉴）：① **任务之间**换模型是自然、干净的切换点，已由 P2（共享上下文）+ P3（任务分派）覆盖——新模型读共享目录即可接力，无需本节机制；② 本节专门解决 **任务中途被迫换人**（卡住/跑偏/估计上下文将满）。

**目标**：当用户觉得某 AI 该换人（卡住/跑偏/估计快到上下文上限），一键把进度交接给另一个空闲 AI 继续。

**新增·改动**：
- `WorkgroupView` 给每个成员加"交接给…"按钮。
- 点击流程：当前 AI 把进度写入 `handoff.md`（模板见下）→ 选一个 idle 同群 AI → 把未完成任务重分派给它 → 新 AI 启动时读 `handoff.md` 从断点继续 → 时间线提示"X→Y 已交接"。
- 可选：在成员卡片显示**输出量指示**（按 ring buffer 字节数粗估，非真实 token）作为"该不该交接"的参考——明确标注"估算值"。

**交接笔记模板**（`handoff.md`）：
```markdown
# 交接笔记 - 任务 XXX
## 已完成 / ## 进行中（当前步骤、已改文件）/ ## 下一步 / ## 注意事项
```

**闸门**：手机上点"交接"→ 写出 handoff.md → 切到 AI-B → AI-B 读 handoff.md 续上 → 任务完成。

**风险**：低（纯手动，无误触发）。未来若真要自动切换，再回到"投入做 parser"那条路（本版已显式排除）。

---

## 4. 阶段依赖与工作量汇总

| 阶段 | 内容 | 想法 | 依赖 | 粗估 |
|---|---|---|---|---|
| P0 | 文档规范 + 竞品推荐 | 4、10 | 无 | ~0.5d |
| P1 | CLI 自动扫描（+补 detect、新增 claude adapter） | 2 | 无 | ~1–1.5d |
| P2 | Workgroup + 共享上下文 | 1 | P1 | ~3–4d |
| P3 | 任务分派 + 跨 AI 读取 | 3 | P2 | ~3d |
| P4 | 四步工作流模板 | 5–8 | P3 | ~3–4d |
| P5 | 手动交接按钮 | 9(降级) | P2、P3 | ~1.5d |

合计约 **12–16 个工作日**。建议先交付 P0–P2（约一周）拿到"手机上能建群+共享上下文"的可演示成果，再决定是否续推 P3–P5。

---

## 5. 与 SPEC 同步要求（SPEC §11 纪律）

落地时 SPEC.md 需同步：
1. §1.1 愿景：补"多 AI 协同工作群"为 v1.x 方向。
2. §3 架构：新增 `WorkgroupManager` 层并画进图。
3. §4 组件：加 §4.6 Workgroup / §4.7 Task / §4.8 Workflow。
4. §4.2 协议：补 `workgroup.*`、`scan.*`、`session.peek` 消息。
5. §7 仓库结构：反映 `docs/` 子目录 + 新增源文件。
6. §8 路线图：把 P0–P5 追加为新 Phase。
7. §9 开放问题：新增"工作群持久化策略""跨 AI 主动 peek 的权限模型"。

---

## 6. 风险与对策（汇总）

| 风险 | 对策 |
|---|---|
| 多 AI 并发写共享文件损坏上下文 | 服务端单写者队列；worktree 隔离；按 agent 分子目录 |
| 自动起多进程占资源 | 默认不自动启动，用户手动加入；设并发上限 |
| 各 CLI 注入方式不统一 | 文件协议（腿 A）为主、initialPrompt（腿 B）为辅；passthrough 退化为提示用户手贴 |
| peek 的裸 ANSI 清洗不完美 | strip-ansi + 标注"近似文本" |
| 工作流自动推进失控 | 默认每步用户确认，强成功标准才自动跳 |
| 范围蔓延（多用户等） | 守住 SPEC §1.2；多 AI ≠ 多用户 |
| 重复造 CCB/amux 的轮子 | 抄 Markdown 文件协议 + worktree 共识；只在"手机优先"上做厚差异化 |

---

## 7. 想法 → 方案 对照表

| 原始想法 | 落点 | 备注 |
|---|---|---|
| 1 多 AI 工作群 + 共享上下文 + 搜竞品 | P2 + §0.2 | 竞品已搜，见 §8 |
| 2 自动扫描 AI CLI + 入群 + 工作界面 | P1 + P2 | detect 全部要新写 |
| 3 任务分派 + 跨 AI 读取 | P3 | peek 走 ring buffer |
| 4 doc 文件夹 + 根目录 md | P0 | |
| 5/6/7/8 规划/执行/审计/修 bug | P4 | 四模板 |
| 9 token 上限自动切换 | P5（降级） | 改手动交接，无 token 数据源 |
| 10 文档加竞品推荐 | P0 | recommended-projects.md |

---

## 8. 竞品推荐与借鉴（想法#10，也即 docs/recommended-projects.md 初稿）

| 项目 | 类型 | 推荐理由 |
|---|---|---|
| CCB / claude_code_bridge | 多 AI CLI 工作台 | 与想法#1 最像：tmux 多 pane + `.ccb` 共享记忆 + `/ask` 委派；学其文件协议与委派模型 |
| amux | 编排 + PWA + kanban | 学其任务看板与 PWA 形态 |
| codeagentswarm / kodo | 并行多 CLI swarm | 学"Markdown 当协议、CLI 互调"的轻量做法 |
| gnap | git 当任务板 | todo/doing/done 用 git 仓库承载，无需常驻编排进程 |
| vibe-kanban | AI 编码 kanban | 任务看板交互参考 |
| slopus/happy | PTY-wrap 架构 | Switchboard 主要灵感来源 |
| go2rtc | 流媒体 | 摄像头模块已用 |
| Tailscale | 组网 | 手机远程访问推荐方式 |

**搜索来源**：
- [AI CLI Agent Swarm (codeagentswarm)](https://www.codeagentswarm.com/en/guides/ai-cli-agent-swarm)
- [awesome-cli-coding-agents](https://github.com/bradAGI/awesome-cli-coding-agents)
- [claude_code_bridge / CCB](https://github.com/bfly123/claude_code_bridge)
- [Agentmaxxing: Parallel Multi-CLI Orchestration](https://codex.danielvaughan.com/2026/04/11/agentmaxxing-parallel-multi-cli-orchestration/)
- [kodo — orchestrator for AI coding](https://github.com/ikamensh/kodo)
- [ComposioHQ/agent-orchestrator](https://github.com/ComposioHQ/agent-orchestrator)
- [awslabs/cli-agent-orchestrator](https://github.com/awslabs/cli-agent-orchestrator)
- [The Code Agent Orchestra — Addy Osmani](https://addyosmani.com/blog/code-agent-orchestra/)

---

## 9. 借鉴 Cursor / Antigravity 的模型切换经验

### 它们为什么"切模型很自然"——本质是上下文归属
- Cursor / Antigravity 是 **API 型工具**：对话、文件、产物、知识库**都归工具自己持有**，模型只是可替换的后端。换模型 = 下一次推理调用换个模型，把工具持有的上下文重新喂进去。
- Cursor 实测：换模型时把**整段历史当纯文本**喂给新模型——机制上"免费"，但代价是各模型 KV cache 不通用会**全部失效**、延迟升高，且**质量下降**（每个模型按自己的上下文风格训练，塞别家对话会"水土不服"）。所以 **Cursor 官方最佳实践：真要换，写一份简明的"代码现状 + 任务 + 约束"摘要，当成带背景的全新开始，而不是原样续接。**
- Antigravity 把这套做成一等原语：**Artifacts**（截图/走查，模型无关的持久产物）+ **Knowledge base**（agent 存取共享记忆，跨任务/模型复用）+ **子 agent workspace 隔离**（不污染主上下文）+ 5 个并行 agent 各挂不同模型。

### 对 Switchboard 的本质差异（决定能借什么、不能照搬什么）
- 我们**驱动 CLI**，不是 API 型工具。每个 CLI（claude/codex）的对话上下文在**它自己进程内**，我们只看得到 PTY 字节流。**我们不是上下文归属方**，所以**做不了也不该做** Cursor 那种"把下一次调用重定向到另一个模型"——那等于自建 LLM 代理，违反 SPEC §1.2。
- 但这恰恰说明：**P5 的"文件交接"不是退而求其次，而是更接近它们的推荐做法**。Cursor 自己都说别原样塞历史、要写摘要——`handoff.md` 就是那份摘要。

### 两种切换场景（都已在方案内）
1. **任务之间切（自然、干净）= P2 + P3 已覆盖**。即你说的"完成一个任务后换模型继续"：任务 A 由模型 X 做完，结果/产物/决策已落 `artifacts/` + `decisions.md` + `context.md`；任务 B 直接分派给模型 Y，Y 读共享上下文开工。**这是我们设计里最干净的切换点，且比 Cursor 原样塞历史更优。**
2. **任务中途切（被迫：卡住/上下文将满）= P5 手动 `handoff.md`**。

### 明确借鉴（已并入对应阶段）
| 借鉴来源 | 经验 | 并入哪里 |
|---|---|---|
| Antigravity Artifacts | 产物是**模型无关的交接货币**，任何切换以产物文件为准、不依赖任一模型内部记忆 | 强化 `artifacts/` 在 P2/P3 的地位 |
| Antigravity Knowledge base | 共享知识库当一等原语，可被任意模型读写 | `context.md` / `decisions.md`；本机已有的 claude-mem 可作喂料源 |
| Cursor 最佳实践 | 交接 = **结构化摘要**而非原样转移 | 校验 P5 `handoff.md` 模板的正确性 |
| Antigravity 子 agent | 每 agent **workspace 隔离** | P3 的 git worktree-per-task |

**搜索来源**：
- [If I change a model, will the new model know the previous context — Cursor Forum](https://forum.cursor.com/t/if-i-change-a-model-will-the-new-model-know-the-previous-context/43939)
- [Why You Shouldn't Switch Models Mid-Conversation — MindStudio](https://www.mindstudio.ai/blog/why-not-switch-models-mid-conversation-ai-coding-agents)
- [Build with Google Antigravity — Google Developers Blog](https://developers.googleblog.com/build-with-google-antigravity-our-new-agentic-development-platform/)
- [Google Antigravity 2.0 Review 2026](https://www.aifloxium.online/blog/google-antigravity-2-0-review-2026)

---

## 10. 多项目管理（读法一：多项目 = 多工作群）

> 你已选 **读法一**（多个独立项目各干各的）。第二问"工作群之上要不要再套 Project 层"已搜索同类项目验证，结论如下。

### 竞品怎么建模（搜索结论）
- **agent-orchestrator (ao)**：`ao project add` 把一个 git repo **注册为 project**，再在其下 `ao spawn` 会话，每个会话一个 git worktree；"worker/orchestrator 默认值按 project 设"。→ **project(=repo) 是一等单元**。
- **Vibe Kanban**：层级是 **Workspace → Repositories → Tasks**，加 repo 即建 worktree，每个 task 跑在独立 worktree。→ 有一个**可选的 Workspace 层**把多个 repo 聚在一起（这正是读法二）。
- **Conductor / Claude Squad**：都以 repo 为单位，agent/task 各自一个 git worktree 隔离。

**共识**：**项目（=一个 git repo / 文件夹）本身就是组织单元**，其下挂多个 agent 会话/任务、用 **git worktree** 隔离；repo 之上一般**不再**套抽象分组（只有可选的 Workspace 把多 repo 捆一起 = 读法二）。

### Switchboard 的决定（与共识一致）
- **工作群 = 一个项目（文件夹）**，**不**另加 "Project" 层——竞品里 repo 本身就是"多 agent 在此运行"的单元，Switchboard 的工作群已扮演这个角色。多项目 = 多工作群。
- 借鉴竞品的"**项目注册表**"思路，把多项目用起来顺手：
  1. 建群默认名 = 文件夹 basename（列表读起来就是"项目列表"）；
  2. 建群提供"**最近项目文件夹**"选择器（持久化 recent-folders）；
  3. **按文件夹去重/续用**：同文件夹已有工作群 → 提示"恢复"而非新建（项目记忆持续累积、不碎片化）；
  4. `SessionList` 每个会话标注所属工作群/项目（多项目时不认错终端）；
  5. 隔离：codex 已隔离 `CODEX_HOME`；antigravity 共用 `~/.gemini`，多项目并发可能冲突 → 真出问题按 per-会话 overlay `HOME`。
- **Workspace（多 repo 聚合）= 读法二，暂不做**。git worktree 隔离留在 P3（任务级），不属于本节。

### 实现落点
- recent-folders 持久化 + 建群按 `cwd` 去重：`workgroup-manager.ts` + `WorkgroupList.tsx`。
- 会话归属标注：`SessionList.tsx`（需要 sessionId → workgroup 的反查）。

**搜索来源**：
- [agent-orchestrator (ao) — registers a git repo as a project](https://github.com/AgentWrapper/agent-orchestrator)
- [Vibe Kanban — Workspaces / Repositories docs](https://vibekanban.com/docs/workspaces/repositories)
- [9 Open-Source Agent Orchestrators for AI Coding (2026)](https://www.augmentcode.com/tools/open-source-agent-orchestrators)
- [Best Git Worktree Tools for AI Coding in 2026](https://nimbalyst.com/blog/best-git-worktree-tools-ai-coding-2026/)
