{
  "name": "switchboard",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hostable web app to drive AI coding CLIs (Claude Code, Codex, ...) from your phone over Tailscale. Plugin-based.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18.18.0"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "concurrently -k -n server,web -c blue,green \"npm run dev -w @switchboard/server\" \"npm run dev -w @switchboard/web\"",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.2",
    "concurrently": "^9.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
