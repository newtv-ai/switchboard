# Switchboard — notes for Claude Code

Project conventions, architecture, and commands live in **[AGENTS.md](./AGENTS.md)** — read it
first. It's the single canonical guide so Claude / Codex / Gemini stay in sync (avoids drift).

`SPEC.md` is the design source of truth; update it before/during any feature work (SPEC §11).

Use this file only for Claude-Code-specific notes; put shared guidance in `AGENTS.md`.
